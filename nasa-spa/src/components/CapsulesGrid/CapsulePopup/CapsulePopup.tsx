import { Capsule } from "../../../interfaces/capsule.interface";

const CapsulePopup = (props: React.PropsWithChildren<{capsule: Capsule}>) => {
  const { capsule } = props;
  return <></>
};

export default CapsulePopup;